#include <tuple>
#include <cmath>

#include "Filter2D.hpp"
#include "geom/Filter2dGeometry.hpp"

namespace nn
{
  /*
  class BConv2dVaildDirectInt8 : public Filter2D
  {
  public:
    //Valid only
    //Multiple of 32 input and 16 output channels
    BConv2dVaildDirectInt8(
        AbstractKernel::Params *akp,
        DerefInputFn *memcpy,
        MatMulBinaryDirectFn *aggregator,
        OTBinaryInt8Out *ot) : Filter2D(akp, memcpy, aggregator, ot) {}
  };

  class BConv2dVaildDirectBin : public Filter2D
  {
  public:
    //Valid only
    //Multiple of 32 input and 16 output channels
    BConv2dVaildDirectBin(
        AbstractKernel::Params *akp,
        DerefInputFn *memcpy,
        MatMulBinaryDirectFn *aggregator,
        OTBinaryBinOut *ot) : Filter2D(akp, memcpy, aggregator, ot) {}
  };
*/
}
